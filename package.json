{
  "name": "eralash-combat-arena-select-15-1-flow-fix",
  "version": "15.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-static.mjs",
    "build": "node scripts/build-static.mjs",
    "set:webhook": "node scripts/set-webhook-local.mjs"
  },
  "engines": {
    "node": "24.x"
  },
  "description": "EraLash Combat Arena Select 15.1: hard pre-fight flow lock, main start always opens character select, then arena select, then fight."
}